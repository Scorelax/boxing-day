name = "boxing-day-submit"
main = "src/index.js"
compatibility_date = "2026-08-22"

[vars]
GITHUB_REPO = "Scorelax/boxing-day"
GITHUB_BRANCH = "main"
ALLOWED_ORIGIN = "https://scorelax.github.io"

# GITHUB_TOKEN is a secret, not a var - set it with:
#   wrangler secret put GITHUB_TOKEN
# (a fine-grained GitHub PAT scoped to just this repo, Contents: read+write)
